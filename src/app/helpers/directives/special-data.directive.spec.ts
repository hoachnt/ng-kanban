import { SpecialDataDirective } from './special-data.directive';

describe('SpecialDataDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialDataDirective();
    expect(directive).toBeTruthy();
  });
});
