import { DropItemSortPipe } from './drop-item-sort.pipe';

describe('DropItemSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DropItemSortPipe();
    expect(pipe).toBeTruthy();
  });
});
