import { KanbanItemsFilterPipe } from './kanban-items-filter.pipe';

describe('KanbanItemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KanbanItemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
